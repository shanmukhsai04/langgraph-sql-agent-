# langgraph-sql-agent-
An AI agent built with LangGraph that translates natural language questions into safe, validated SQL queries, executes them, and auto-corrects failures — powered by GPT-4o-mini.
